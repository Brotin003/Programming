// Write a programm to calculate simple intrest for a set of values representing principle,
// number of years and rate of intrest
#include <stdio.h>
int main()
{
    int principle = 100, rate = 4, years = 1;
    int simpleIntrest = (principle* rate* years)/100;
    printf("The value of simple intrest is %d",simpleIntrest);
    

    return 0;
}



















