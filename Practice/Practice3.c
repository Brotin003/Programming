#include <stdio.h>

int main()

{
    printf("%10u", sizeof(char));

    return 0;
}

                            // to find the memory or size taken by int, char etc in RAM
                            // (%u) = it is for position of value in result